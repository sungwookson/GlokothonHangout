var router = require("express").Router();

router.get('/users/info', function(res, req){
    /* 
        UID, 
        email,
        nickname, 
        picture,
        detail 
    */
    
})

module.exports = router;