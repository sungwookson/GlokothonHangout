let router = require('express').Router();

/**
 * :::::: 사용자가 가장 최근에 등록한 약속을 기준으로 조회
 */
router.route('/promises').get(function (req, res) {

});

/**
 * :::::: 새로운 약속 등록
 * uid,
 * startDate,
 * endDate,
 * location,
 * plan
 */
router.route('/promises').post(function (req, res) {
    
});